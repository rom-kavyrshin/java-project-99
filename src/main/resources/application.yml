spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  output:
    ansi:
      enabled: always
  application:
    name: "app"

default-user:
  password: ${DEFAULT_PASSWORD}

rsa:
  public-key: classpath:certs/public.pem
