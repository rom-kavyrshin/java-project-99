spring:
  profiles:
    active: test
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  output:
    ansi:
      enabled: always
  application:
    name: "app"

default-user:
  password: "a123"

rsa:
  public-key: classpath:certs/public.pem
  private-key-path: certs/private.pem.enc
