spring:
  profiles:
    active: test
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  output:
    ansi:
      enabled: always
  application:
    name: "app"

default-user:
  password: "a123"

rsa:
  public-key: classpath:certs/public.pem
  private-key-path: classpath:certs/private.pem.enc

PRIVATE_KEY_PASSWORD: weaKPa55w0rd
PRIVATE_KEY_SALT: c036d7ae55bcaf25
